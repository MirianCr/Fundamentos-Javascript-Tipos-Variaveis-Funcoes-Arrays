const notas = [10, 6, 8] // 7
notas.push(7)
console.log(notas)

let media = (notas[0] + notas[1] + notas[2] + notas[3])/notas.length
console.log(media)

//push chamamos de push, ou seja, ele vai colocar no último local, no último elemento do nosso array uma nova nota. Ele vai criar esse elemento e colocar nota.